--index-url https://download.pytorch.org/whl/cpu

# Install this file first with --no-deps. Every Torch dependency is pinned in
# requirements.txt, which then sees this CPU wheel satisfying sentence-transformers.
torch==2.12.0+cpu
